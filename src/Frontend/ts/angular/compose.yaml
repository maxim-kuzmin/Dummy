services:
  angular-prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: maxim-kuzmin.makc_dummy_production_frontend_web_angular
    ports:
      - "8080:8080"

  # angular-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dev
  #   image: maxim-kuzmin.makc_dummy_development_frontend_web_angular
  #   ports:
  #     - "4200:4200"
  #   develop:
  #     watch:
  #       - action: sync
  #         path: .
  #         target: /app
